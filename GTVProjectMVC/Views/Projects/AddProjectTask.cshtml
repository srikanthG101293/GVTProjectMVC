@model ModelEntity.ProjectModel
@{
    Layout = null;
}

<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/css/bootstrap.min.css">
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.5.1/jquery.min.js"></script>
    <script src="https://cdnjs.cloudflare.com/ajax/libs/popper.js/1.16.0/umd/popper.min.js"></script>
    <script src="https://maxcdn.bootstrapcdn.com/bootstrap/4.5.0/js/bootstrap.min.js"></script>
    <script src="~/Scripts/Custom/ProductDetails.js?v=1.2"></script>
</head>
<body>
    @*@using (Html.BeginForm("ProjectsDisplay", "Home", FormMethod.Post))
        {*@
    <div class="container">
        <h2>Add Tasks for Projects</h2>
        <form>
            <div class="form-group">
                <label>Task Title:</label>

                @*@Html.DropDownListFor(x => x.ProjectID, new SelectList(ViewBag.p, "ID", "Name"))*@

                <input type="email" class="form-control" id="txtTaskTitle" placeholder="Enter Task Title">
                <span id="spntaskTile" style="color:red"></span>
            </div>
            <div class="form-group">
                <label>Select Project:</label>
                <select class="form-control" id="ddlProject">
                    <option value="0">-Select Project-</option>
                    @foreach (var item in Model.ProjectDetails)
                    {
                        <option value="@item.ProjectID">@item.ProjectTitle</option>
                    }
                </select>
                <span id="spnProject" style="color:red"></span>
            </div>
            <div class="form-group">
                <label for="email">Add Hours:</label>
                <input type="email" class="form-control" id="txtHours" placeholder="Enter Hours">
                <span id="spnHours" style="color:red"></span>
            </div>
            <input type="button" class="btn btn-success" value="Submit" id="btnsaveTask">
        </form>
        <h2 id="successText" style="color:green"></h2>
        <a type="button" class="btn btn-primary" href="/Projects/ProjectsTaskDisplay">View All Project Details</a>
        <a type="button" class="btn btn-primary" href="/Projects/AddProject">Add Project</a>
    </div>
    
    @*}*@

</body>
</html>

